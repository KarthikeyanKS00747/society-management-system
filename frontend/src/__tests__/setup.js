/**
 * Vitest global setup
 * – imports @testing-library/jest-dom so custom matchers like
 *   toBeInTheDocument, toBeDisabled, toHaveValue, etc. are available
 *   in every test file automatically.
 */
import '@testing-library/jest-dom';
